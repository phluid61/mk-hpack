# Database

This project does not use a database. mkhpack is a stateless C library that
operates entirely on in-memory byte buffers provided by the caller. There is no
persistent data storage, file-based state, or external data service.
